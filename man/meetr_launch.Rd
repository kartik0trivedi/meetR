% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch.R
\name{meetr_launch}
\alias{meetr_launch}
\title{Launch the meetR availability app}
\usage{
meetr_launch(
  csv,
  event_name,
  app_title = "meetR",
  timezone = Sys.timezone(),
  sheet_id = Sys.getenv("MEETR_SHEET_ID"),
  deploy = FALSE,
  app_name = .slugify(app_title),
  ...
)
}
\arguments{
\item{csv}{Path to the availability CSV file.}

\item{event_name}{Display name for the event shown to respondents.}

\item{app_title}{Title shown in the browser tab / app header.
Defaults to \code{"meetR"}.}

\item{timezone}{An Olson timezone name (e.g. \code{"America/Chicago"},
\code{"Europe/London"}, \code{"UTC"}). Displayed in the app so respondents know
which timezone the hours refer to. Defaults to the system timezone via
\code{Sys.timezone()}.}

\item{sheet_id}{Google Sheets spreadsheet ID. Defaults to the value stored
by \code{\link[=meetr_auth]{meetr_auth()}}.}

\item{deploy}{If \code{TRUE}, deploy the app to shinyapps.io instead of running
locally. Requires the \pkg{rsconnect} package to be configured.}

\item{app_name}{shinyapps.io app name (used when \code{deploy = TRUE}).
Defaults to a slugified version of \code{app_title}.}

\item{...}{Additional arguments passed to \code{shiny::runApp()} (when
\code{deploy = FALSE}) or \code{rsconnect::deployApp()} (when \code{deploy = TRUE}).}
}
\value{
Invisible \code{NULL}. Called for its side effects.
}
\description{
Reads the organiser's availability CSV, saves the event to Google Sheets,
then either runs the Shiny app locally or deploys it to shinyapps.io.
}
\details{
The CSV must have three columns:
\describe{
\item{date}{Date in \code{YYYY-MM-DD} format.}
\item{start}{Start hour (integer, 0–23, 24-hour clock).}
\item{end}{End hour (integer, 1–24, exclusive). Must be > start.}
}
Multiple rows per date are allowed (e.g. morning + afternoon blocks).
}
\examples{
\dontrun{
meetr_auth("YOUR_SHEET_ID")

# Use the bundled sample CSV as a starting point
sample_csv <- system.file("extdata", "sample_availability.csv", package = "meetR")

# Run locally
meetr_launch(sample_csv, event_name = "Team Sync Q2")

# Deploy to shinyapps.io
meetr_launch(sample_csv, event_name = "Team Sync Q2", deploy = TRUE)
}

}
